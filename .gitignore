# Secrets — never commit
.env

# Generated at runtime by launch.sh — not a source file
docker-compose.generated.yml

# Local Claude context files
CLAUDE.md
NewChatPrompt.md

# macOS / editor noise
.DS_Store
*.swp
*.bak
